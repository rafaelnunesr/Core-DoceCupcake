extension APIErrorMessage {
    enum Credentials {
        static let invalidCredentials = "Invalid email or password"
        static let userAlreadyRegistered = "User already has an account"
    }
}
